# HX-CFD-QUAL-01 v7 combined flow story

This is the single browser recording for the retained visualization package. It combines three derived views into one 15-second presentation:

1. **Flow entry (0–5 s):** the retained `t=800 s` streamlines are progressively revealed from an empty view at the left-side inlets toward the outlets.
2. **Retained flow (5–10 s):** the actual field-derived streamline snapshots at 0, 200, 400, 600, and 800 s.
3. **Thermal gradient (10–15 s):** the same retained snapshot sequence, with streamlines colored by the retained `T` field on a fixed 435–600 K scale.

The thermal colors are intentionally more contrastive for readability, but the Kelvin values, geometry, and solver fields are unchanged. The combined MP4/GIF is a presentation artifact; it does not add solver time steps, create a zero-initialized transient, or change the open numerical disposition.

