# HX-02 blind plan

The custodian redacts and hashes the visible packet first. Luna/Aero then freezes its plan, analytical prediction, geometry, mesh, solver settings, numerical gates, and engineering disposition. Only after that freeze may the custodian digitize and reveal predeclared experimental points. Any post-reveal tuning receives a new run ID.

The benchmark can stop before CFD if the manifold/corrugation topology or required properties cannot be reconstructed without unbounded guessing. A stop is retained as evidence; it is not silently repaired into a validation claim.

